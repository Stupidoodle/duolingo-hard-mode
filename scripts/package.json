{
	"name": "duolingo-hard-mode-scripts",
	"private": true,
	"type": "module",
	"description": "Local-only dev tooling. Kept out of the root package.json so CI's npm lockfile stays untouched.",
	"dependencies": {
		"playwright-core": "^1.55.0"
	}
}
